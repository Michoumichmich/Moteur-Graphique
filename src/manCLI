Supported commands :
  help
  stop, exit, quit
  init
      ge
      env <environment name>
  list
      env
      objects
      cameras
      lights
  set
      object <object number>
            transform <scale> <x_rot> <y_rot> <z_rot> <x_pos> <y_pos> <z_pos>
            color <r> <g> <b>
            reflexivity <reflexivity : between 0 and 1>
            light_intensity <light_intensity : between 0 and 1>
      env <environment name>
      camera
      haze <intensity : between 0 and 1>
      background <on/off>
      render_mode <standard/depthmap/bitmap>
      resolution <resolution>
      reflexion <on/off>
  add
      <object> <x> <y> <z> <size> [<r> <g> <b>]
      light <x> <y> <z> <intensity>
      camera <x> <y> <z> <camera name> <x_target> <y_target> <z_target>
  reset <environment name>
  render [filename.bmp]
  demo
